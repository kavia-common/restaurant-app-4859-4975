package com.example.mobile_frontend

import io.flutter.embedding.android.FlutterActivity

// PUBLIC_INTERFACE
/**
 * MainActivity is the Android entry point for the Flutter application, using
 * Android v2 embedding (FlutterActivity).
 *
 * Parameters: None
 * Returns: The activity hosts the Flutter UI; no explicit return value.
 */
class MainActivity : FlutterActivity()
