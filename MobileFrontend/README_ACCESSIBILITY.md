# Accessibility Notes

- Color contrast uses warm palette with dark text on light cream surfaces.
- Touch targets use Material chips and buttons with sufficient sizes.
- Screen reader friendly: Use semantic labels and alt text when integrating real images.
- Text scaling: Application inherits device font scaling; verify layouts at 200%.
- For full WCAG: Add semantics widgets to dynamic content, and ensure focus order in forms.
