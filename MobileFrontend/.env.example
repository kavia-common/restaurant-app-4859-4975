# Example environment file for MobileFrontend (do not commit actual .env)
# PUBLIC_INTERFACE
# BACKEND_API_BASE_URL is the base URL of the backend API the app communicates with.
# e.g., http://localhost:3000 or https://api.example.com
BACKEND_API_BASE_URL=https://your-backend.example.com
